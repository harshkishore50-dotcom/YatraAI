window.YATRA_CONFIG = {
  // Deploy api/yatraai.js on Vercel / another serverless host.
  // Paste ONLY the public endpoint URL here — never an API key.
  AI_ENDPOINT: 'https://YOUR-VERCEL-APP.vercel.app/api/yatraai',
  OVERPASS_ENDPOINTS: [
    'https://overpass-api.de/api/interpreter',
    'https://overpass.kumi.systems/api/interpreter'
  ]
};
